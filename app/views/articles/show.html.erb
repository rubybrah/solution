<%= link_to 'Back to article listing', articles_path, class: "right" %>
<h3><%= @article.name %></h3>
<p class="subheader">
  <%= markdown(@article.content) %>
</p>
<%= link_to 'Back to article listing', articles_path, class: "right" %>

<% if admin_signed_in? %>
  <%= link_to 'Edit', edit_article_path(@article), class: "button" %>

<% end %>